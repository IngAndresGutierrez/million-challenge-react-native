import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  searchbar: {
    marginBottom: 16,
    borderRadius: 8,
    backgroundColor: '#fff',
    borderWidth: 1,
    borderColor: '#e0e0e0',
  },
})
